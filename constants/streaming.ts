export enum StreamingFormat {
    CSV = 'csv',
    JSON = 'json',
    TEXT = 'text',
}

export enum StreamingGranularity {
    REAL_TIME = 'realTime',
    HIGH = 'high',
    MEDIUM = 'medium',
    LOW = 'low',
    UNKNOWN = 'unknown',
}
